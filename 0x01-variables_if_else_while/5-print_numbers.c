#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - print single digit number of base 10
 * Return: Always (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	printf("%d", i);
	printf("\n");
	return (0);
}
